const theme = {
  currentLabel: 'Theme: {theme}',
  switchedTo: 'Switched to {theme} theme',
  options: {
    light: 'light',
    dark: 'dark',
    system: 'system',
  },
} as const

export default theme
